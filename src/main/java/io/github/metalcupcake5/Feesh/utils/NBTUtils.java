package io.github.metalcupcake5.Feesh.utils;

import io.github.metalcupcake5.Feesh.items.ItemType;
import net.minecraft.server.v1_8_R3.NBTTagCompound;
import org.bukkit.inventory.ItemStack;

public class NBTUtils
{
    /*public static boolean isItemType(ItemStack stack, ItemType type)
    {
        if (stack.hasTag() && stack.getTag().hasString("custom_id"))
            return stack.getTag().getString("custom_id").equals(type.getId());
        return false;
    }

    NBTTagCompound*/
}